services:
  # Landing Page
  landing:
    build:
      context: .
      dockerfile: Dockerfile.landing
    container_name: query-builder-landing
    restart: unless-stopped
    ports:
      - "5012:5012"
    networks:
      - landing-network

networks:
  landing-network:
    driver: bridge
